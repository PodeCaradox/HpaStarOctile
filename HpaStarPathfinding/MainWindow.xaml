<Window
    Background="White"
    Name="PathfindingWindow"
    Title="Pathfinding Visualizer"
    UseLayoutRounding="True"
    mc:Ignorable="d"
    x:Class="HpaStarPathfinding.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:HpaStarPathfinding.ViewModel"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>
    <StackPanel Margin="4" Orientation="Horizontal">
        <Grid>
            <Grid.Resources>
                <Style TargetType="Expander">
                    <Setter Property="Padding" Value="0,0,0,10" />
                </Style>
            </Grid.Resources>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Expander
                Background="White"
                ExpandDirection="Down"
                Grid.Column="0"
                Grid.Row="0"
                HorizontalAlignment="Left"
                IsExpanded="True">
                <Expander.Header>
                    <Grid Height="26" Width="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Expander}}, Path=ActualWidth}">
                        <Rectangle Fill="LightGray" />
                        <TextBlock Padding="5,0,0,0" VerticalAlignment="Center">Pathfinding:</TextBlock>
                    </Grid>
                </Expander.Header>
                <StackPanel>
                    <DockPanel
                        HorizontalAlignment="Stretch"
                        Margin="0,0,0,10"
                        VerticalAlignment="Center">
                        <TextBlock
                            HorizontalAlignment="Stretch"
                            Margin="0,0,5,0"
                            Text="Pathfinding Algorithm: "
                            VerticalAlignment="Center" />
                        <ComboBox
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            ItemsSource="{Binding algorithms}"
                            MinWidth="100px"
                            SelectedItem="{Binding selectedAlgorithm}"
                            SelectionChanged="Selector_OnSelectionChanged"
                            x:Name="PathfindingAlgorithmComboBox">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel
                                        HorizontalAlignment="Stretch"
                                        Orientation="Horizontal"
                                        VerticalAlignment="Center">
                                        <Rectangle
                                            Fill="{Binding Brush}"
                                            Height="16"
                                            Stroke="Black"
                                            StrokeThickness="1"
                                            Width="16" />
                                        <TextBlock Margin="10,0,0,0" Text="{Binding Name}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </DockPanel>
                    <ToggleButton
                        Checked="ChangePathToggleButton_OnChecked"
                        Content="Path Mode (Left / Right Mouse Button)"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        IsChecked="{Binding changePathfindingNodeEnabled, Mode=TwoWay}"
                        x:Name="ChangePathToggleButton" />
                    <TextBlock HorizontalAlignment="Center" Margin="0,10,0,10">
                        Edit Cell (Right Click):
                    </TextBlock>
                    <Border
                        BorderBrush="Yellow"
                        BorderThickness="1"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                        <Image
                            Height="{Binding .multipliedCellSize}"
                            MaxHeight="{Binding .multipliedCellSize}"
                            MaxWidth="{Binding .multipliedCellSize}"
                            MinHeight="{Binding .multipliedCellSize}"
                            MinWidth="{Binding .multipliedCellSize}"
                            MouseLeftButtonDown="ChangeCellBorderClicked"
                            RenderOptions.BitmapScalingMode="NearestNeighbor"
                            RenderOptions.EdgeMode="Aliased"
                            SnapsToDevicePixels="True"
                            Source="{Binding currentSelectedCellSource}"
                            Width="{Binding .multipliedCellSize}" />
                    </Border>
                </StackPanel>
            </Expander>
            <Expander
                Background="White"
                ExpandDirection="Down"
                Grid.Row="1"
                HorizontalAlignment="Left"
                IsExpanded="True">
                <Expander.Header>
                    <Grid Height="26" Width="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Expander}}, Path=ActualWidth}">
                        <Rectangle Fill="LightGray" />
                        <TextBlock Padding="5,0,0,0" VerticalAlignment="Center">Map:</TextBlock>
                    </Grid>
                </Expander.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock VerticalAlignment="Center">Map Size:</TextBlock>
                    <TextBox
                        Grid.Column="1"
                        Grid.Row="0"
                        PreviewTextInput="Integer"
                        Text="{Binding uiMapX, Mode=TwoWay}"
                        VerticalContentAlignment="Center" />
                    <TextBox
                        Grid.Column="2"
                        Grid.Row="0"
                        PreviewTextInput="Integer"
                        Text="{Binding uiMapY, Mode=TwoWay}"
                        VerticalContentAlignment="Center" />
                    <Button
                        Click="ClearClicked"
                        Content="Create Map"
                        Grid.Column="3"
                        Grid.Row="0"
                        Padding="5,5,5,5"
                        x:Name="ClearPathToggleButton" />

                </Grid>
            </Expander>
            <Expander
                Background="White"
                ExpandDirection="Down"
                Grid.Column="0"
                Grid.Row="2"
                HorizontalAlignment="Left"
                IsExpanded="True">
                <Expander.Header>
                    <Grid Height="26" Width="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Expander}}, Path=ActualWidth}">
                        <Rectangle Fill="LightGray" />
                        <TextBlock Padding="5,0,0,0" VerticalAlignment="Center">Debug:</TextBlock>
                    </Grid>
                </Expander.Header>
                <StackPanel>
                    <CheckBox
                        Checked="DrawChunksButtonChecked"
                        Content="Draw Chunks"
                        Margin="0,10,0,0"
                        Unchecked="DrawChunksButtonUnchecked"
                        x:Name="DrawChunksButton" />
                    <CheckBox
                        Checked="DrawPortalsButtonChecked"
                        Content="Draw Portal Entrances"
                        Unchecked="DrawPortalsButtonUnchecked"
                        x:Name="DrawPortalsButton" />

                    <CheckBox
                        Checked="DrawPortalsInternalConnectionsChecked"
                        Content="Show Internal Connections"
                        Unchecked="DrawPortalsInternalConnectionsUnchecked"
                        x:Name="DrawPortalsConnectionsButton" />
                    <CheckBox
                        Checked="DrawPortalsExternalConnectionsChecked"
                        Content="Show External Connections"
                        Unchecked="DrawPortalsExternalConnectionsUnchecked"
                        x:Name="DrawPortalsExternalConnectionsButton" />
                </StackPanel>
            </Expander>
            <Expander
                Background="White"
                ExpandDirection="Down"
                Grid.Column="0"
                Grid.Row="3"
                HorizontalAlignment="Left"
                IsExpanded="True">
                <Expander.Header>
                    <Grid Height="26" Width="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Expander}}, Path=ActualWidth}">
                        <Rectangle Fill="LightGray" />
                        <TextBlock Padding="5,0,0,0" VerticalAlignment="Center">Shortcut Keys:</TextBlock>
                    </Grid>
                </Expander.Header>
                <StackPanel>
                    <TextBlock>T: Toggle Edit / Path Mode</TextBlock>
                    <TextBlock>P: Toggle Pathfinding Algorithm</TextBlock>
                    <TextBlock>C: Reset</TextBlock>
                </StackPanel>
            </Expander>
        </Grid>
        <StackPanel />
        <Viewbox Stretch="UniformToFill">
            <Canvas Background="White" Name="PathCanvas">
                <!--  Content will be added dynamically  -->
            </Canvas>
        </Viewbox>
    </StackPanel>
</Window>