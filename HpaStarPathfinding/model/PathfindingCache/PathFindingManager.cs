using System.Collections.Generic;

namespace HpaStarPathfinding.ViewModel
{
    public class PathFindingManager
    {
        //use tick as the sorting and portal key start/goal as a input to get cached path, only most frequently used paths are cached, older ones on older ticks will get removed after hitting 1000 cached paths
    }
}